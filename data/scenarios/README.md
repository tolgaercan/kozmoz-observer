# Aktif senaryo

| Dosya | Açıklama |
|-------|----------|
| `api-watcher-attach.json` | Chrome attach → JWT bootstrap → GetClosedDate poll |

Eski UI/register senaryoları: `archive/legacy-src/scenarios/data/`
