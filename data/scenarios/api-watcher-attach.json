{
  "id": "api-watcher-attach",
  "name": "API GetClosedDate watcher (attach)",
  "description": "CDP attach → JWT (yoksa Kosmos ana sayfa) → GetClosedDate poll. Wizard navigasyonu yok.",
  "status": "active",
  "banSafe": true,
  "note": "Chrome debug acik → panelden API Watcher. JWT yoksa kosmosvize.com.tr → appointmentForm (sifre/OTP).",
  "steps": [
    {
      "phase": "chrome-login",
      "label": "CDP bağlan",
      "params": {
        "attachOnly": true,
        "skipSessionInject": true
      }
    },
    {
      "phase": "api-auth-bootstrap",
      "label": "JWT (gerekirse ana sayfa)",
      "params": {
        "apiOnly": true
      }
    },
    {
      "phase": "api-watcher",
      "label": "GetClosedDate API poll",
      "params": {
        "apiOnly": true
      }
    }
  ]
}
