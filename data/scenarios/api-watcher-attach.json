{
  "id": "api-watcher-attach",
  "name": "API GetClosedDate watcher (attach)",
  "description": "CDP attach → JWT oku → GetClosedDate poll. Navigasyon/wizard yok; ofis ve başvuru şekli yalnızca API parametresi.",
  "status": "experimental",
  "banSafe": true,
  "note": "Chrome debug acik + portal oturumu → panelden ofis/şekil sec → API Watcher. Sayfa degistirilmez.",
  "steps": [
    {
      "phase": "chrome-login",
      "label": "CDP bağlan",
      "params": {
        "attachOnly": true,
        "skipSessionInject": true
      }
    },
    {
      "phase": "api-auth-bootstrap",
      "label": "JWT oku (navigasyon yok)",
      "params": {
        "apiOnly": true
      }
    },
    {
      "phase": "api-watcher",
      "label": "GetClosedDate API poll",
      "params": {
        "apiOnly": true
      }
    }
  ]
}
